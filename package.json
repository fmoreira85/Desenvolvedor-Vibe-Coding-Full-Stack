{
  "name": "sdr-crm",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "supabase": "^2.98.2",
    "ws": "^8.20.0"
  }
}
